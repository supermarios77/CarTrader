distributor:
  receivers:
    otlp:
      protocols:
        grpc:
        http:

compactor:
  compaction:
    block_retention: 168h

storage:
  trace:
    backend: local
    wal:
      path: /tempo/wal
    local:
      path: /tempo/traces
server:
  http_listen_port: 3200
